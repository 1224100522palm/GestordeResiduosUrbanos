<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISOS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestordeResiduosUrbanos">

        <!-- ACTIVIDAD PRINCIPAL -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GestordeResiduosUrbanos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OTRAS ACTIVIDADES -->
        <activity android:name=".ui.LoginActivity" android:exported="false" />
        <activity android:name=".ui.RegistroActivity" android:exported="false" />
        <activity android:name=".ui.PantallaMapaActivity" android:exported="false" />
        <activity android:name=".ui.PantallaBotesActivity" android:exported="false" />
        <activity android:name=".ui.PantallaRegistroBoteActivity" android:exported="false" />
        <activity android:name=".ui.PantallaHorarioActivity" android:exported="false" />
        <activity android:name=".ui.PantallaRegistroHorarioActivity" android:exported="false" />
        <activity android:name=".ui.PantallaReportesActivity" android:exported="false" />
        <activity android:name=".ui.PantallaStatsActivity" android:exported="false" />
        <activity android:name=".ui.PantallaRegistroReporteActivity" android:exported="false" />
        <activity android:name=".ui.PantallaRegistroAvisoActivity" android:exported="false" />
        <activity android:name=".ui.PantallaAvisoActivity" android:exported="false" />

        <!-- GOOGLE MAPS API KEY -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDrDbhkcbUg5SUybDYUj1FVGhKkeDM_kLE" />

        <!-- OBLIGATORIO PARA MAPAS (GOOGLE PLAY SERVICES VERSION) -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>
